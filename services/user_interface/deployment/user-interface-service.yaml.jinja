apiVersion: v1
kind: Service
metadata:
  name: "user-interface-svc"
  namespace: {{ kube_namespace }}
  labels:
    app: "user-interface"
spec:
  ports:
    - port: 3000
      targetPort: 3000
      protocol: "TCP"
      name: http
  selector:
    app: "user-interface"
  type: "NodePort"
