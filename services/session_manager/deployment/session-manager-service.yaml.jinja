apiVersion: v1
kind: Service
metadata:
  name: "session-manager-svc"
  namespace: {{ kube_namespace }}
  labels:
    app: "session-manager"
spec:
  ports:
    - port: 8100
      targetPort: 8100
      protocol: "TCP"
      name: http
  selector:
    app: "session-manager"
  type: "NodePort"
